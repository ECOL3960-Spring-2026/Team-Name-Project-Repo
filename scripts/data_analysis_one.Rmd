---
title: "data_analysis_one"
author: "Kade Stewart"
date: "2026-03-19"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r}
library(tidyverse)
library(here)

```

```{r}
fish <- fish %>% 
  drop_na(specimenNumber) %>% 
  group_by(domainID, siteID, month, year) %>% 
  summarise(fish_month = sum(specimenNumber))

plant <- plant %>% 
  drop_na(aquaticPlantType) %>% 
  group_by(domainID, siteID, month, year) %>% 
  summarise(plant_month = count(aquaticPlantType))

discharge <- discharge %>% 
  drop_na(dischargeContinuous) %>% 
  group_by(domainID, siteID, month, year) %>% 
  summarise(discharge_month = sum(dischargeContinuous))

```

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

